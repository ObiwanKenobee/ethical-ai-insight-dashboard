
import React from 'react';
import Sidebar from '@/components/Sidebar';
import Dashboard from '@/pages/Dashboard';

const Index = () => {
  // We don't need this component since App.tsx already handles routing and layout
  return <Dashboard />;
};

export default Index;
